App.ApplicationController = Ember.Controller.extend({

	// needs: ['users'],
	// usersCount : function() {
	//     return this.get('controllers.users').get('model.length');
	// }.property('@each'),

	// inflection : function() {
	//     var contact = this.get('usersCount');
	//     return contact === 1 ? 'Contact' : 'Contacts';
	// }.property('usersCount'),
});
